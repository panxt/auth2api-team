node_modules/
dist/
config.yaml
*.log
.env

# Internal-only docs (contain API keys / internal network info)
docs/INTERNAL_SETUP.md

# Per-user onboarding manuals (each contains a unique API key) + config backups
out/
config.yaml.bak.*

# Admin script private config (contains admin API key)
.auth2api-admin.env

# Editor / IDE local state
.cursor/
.idea/
.vscode/
*.tsbuildinfo

# OS metadata
.DS_Store
Thumbs.db
